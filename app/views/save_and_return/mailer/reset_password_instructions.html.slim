p = t('.someone_requested')
p = t('.not_be_changed')

p
  = link_to t('.reset'), edit_password_url(@resource, reset_password_token: @token, locale: I18n.locale == :en ? nil : I18n.locale)

p = t('.didnt_request')