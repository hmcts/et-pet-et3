language: ruby
cache:
  bundler: true
  directories:
  - tmp/parallel_runtime_log
before_script:
  - bundle exec rake parallel:create[4] parallel:migrate[4]
script:
  - bundle exec rake parallel:spec[4]
addons:
  chrome: stable
  firefox: latest
  postgresql: "9.3"
services:
  - postgresql